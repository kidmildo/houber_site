# ==========================================================================
# .assetsignore — 사이트에 올리지 않을 파일 목록
# --------------------------------------------------------------------------
# 개발용 문서와 설정 파일은 방문자에게 공개할 이유가 없으므로 제외합니다.
# (_headers 는 설정으로 쓰이므로 Cloudflare가 알아서 제외합니다)
# ==========================================================================

README.md
wrangler.jsonc
.assetsignore
